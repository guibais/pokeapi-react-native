import { styled } from "stitches-native";

export const ListingHeader = styled("View", {
  marginBottom: 20,
  display: "flex",
  flexDirection: "row",
  justifyContent: "space-between",
  alignContent: "center",
});

export const ListingHeaderTitle = styled("Text", {
  fontSize: 18,
  fontWeight: "bold",
});
